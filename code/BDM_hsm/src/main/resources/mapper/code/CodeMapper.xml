<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.bdm.code">
    <select id="doRetrieve"  resultType="CodeVO">
		SELECT
    		category,
    		divs,
    		div_name,
    		seq,
    		use_yn
		FROM
    		code
    </select>

</mapper>